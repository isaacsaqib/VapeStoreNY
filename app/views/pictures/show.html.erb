

<title>MERCY X MANKIND NY</title>

<%= form_for @picture do |f| %>

    <%= image_tag @picture.image.url, :class => "picture-edit" %><br>
    
    <% if logged_in? %>
    <%= f.label :description %>
    <%= f.text_field :description%><br>
  	<%= link_to "Delete picture", @picture, :method => :delete,
                                      data: { confirm: "You sure?" } %>

  
 
    <%= f.submit  %>
    <% end %>

<% end %>

